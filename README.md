# 🥗 Receipe Finder App

Explore and discover recipes effortlessly based on their food preferences — whether it’s breakfast, lunch, dinner, veg, or non-veg.
Choose your category, and get a curated list of delicious recipes instantly!

![receipe-finder-home](./images/receipe-finder-home-page.png)

# Features:

- Used the `App Router` for routing between different pages in the application.

- Used a route group to organize different kinds of recipes `(recipes)`.

- Used `next/image` to load optimized background images on each page.

- Used a dedicated `layout` page to provide layouts for different categories.

- Used `not-found.tsx` to catch all unmatched routes as a fallback page.
